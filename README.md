# FreeMusic

Приложение для бесплатного прослушивания музыки на вашем ПК(позже смартфоне). Позволяет искать и воспроизводить треки из различных источников.

## ✨ Возможности
*   Поиск и потоковое воспроизведение музыки с YouTubeи других платформ.
*   Локальная библиотека треков (в разработке).
*   Простой и удобный веб-интерфейс.

## 🚀 Быстрый старт
### Предварительные требования
*   Python 3.8+
*   pip (менеджер пакетов Python)

### Установка и запуск
1.  Клонируйте репозиторий:
    ```bash
    git clone https://github.com/Magmucot/FreeMusic.git
    cd FreeMusic
    ```
2.  Установите зависимости для бэкенда (предположительно):
    ```bash
    pip install -r backend/requirements.txt
    ```

3.  Запустите бэкенд-сервер (укажите правильную команду):
    ```bash
    python backend/main.py
    ```
4.  Откройте фронтенд. Для этого просто откройте в браузере файл `frontend/index.html`.

## 📁 Структура проекта
*   `backend/` — исходный код серверной части (логика поиска, API).
*   `frontend/` — исходный код клиентской части (веб-интерфейс).
*   `songs/` — Папка с локальной музыкой.
*   `music_lib.db` — SQLite база данных для локальной библиотеки треков.
*   `LICENSE` — файл лицензии (уже есть).


## 📄 Лицензия
Этот проект распространяется под лицензией GPVL V3. Подробнее см. в файле [LICENSE](LICENSE).